{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["@upstash/context7-mcp"],
      "description": "Up-to-date library docs for LLMs — auto-resolves versions"
    },
    "tavily": {
      "command": "npx",
      "args": ["-y", "tavily-mcp"],
      "env": {
        "TAVILY_API_KEY": "YOUR_TAVILY_API_KEY"
      },
      "description": "Tavily AI search — search, extract, crawl, map"
    },
    "codebase-memory": {
      "command": "npx",
      "args": ["-y", "codebase-memory-mcp"],
      "description": "Persistent codebase knowledge graph"
    },
    "github": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-e",
        "GITHUB_PERSONAL_ACCESS_TOKEN",
        "ghcr.io/github/github-mcp-server"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "YOUR_GITHUB_PAT"
      },
      "description": "GitHub API — repos, issues, PRs, actions, code search"
    },
    "docker": {
      "command": "docker",
      "args": ["mcp", "gateway", "run"],
      "description": "Docker MCP Toolkit — requires Docker Desktop 4.62+"
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--headless"],
      "description": "Playwright browser automation"
    },
    "markdownify": {
      "command": "npx",
      "args": ["-y", "markdownify-mcp"],
      "description": "Convert PDFs, images, audio into Markdown"
    },
    "mcphub": {
      "type": "http",
      "url": "http://localhost:3000/mcp",
      "description": "MCPHub — manage multiple MCP servers via HTTP dashboard"
    }
  }
}
